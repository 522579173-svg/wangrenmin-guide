# 📖 全国景点讲解 App · 软件制作流程与维护手册

> 本手册记录这个 App 的**完整制作流程、技术架构、部署方式、维护方法**，方便以后自己维护、升级。
> 最后更新：2026-08-13

---

## 一、项目是什么

一个「像手机 App 一样」的**网页应用（PWA）**，核心功能是**全国景点讲解 + 仿真人导游 + AI 自动生成讲解**。

| 特性 | 说明 |
|---|---|
| 形态 | 单文件网页应用（核心就是 `index.html`） |
| 平台 | 安卓 / 苹果 / 电脑浏览器都可用 |
| 安装方式 | 浏览器「添加到主屏幕」，桌面出现图标，像原生 App |
| 语音 | 浏览器自带中文 TTS（免费、离线可播） |
| 定位 | 手机 GPS（需 HTTPS 环境） |
| AI 讲解 | DeepSeek API（服务器端代理，Key 安全存放） |
| 定位识别 | 高德逆地理编码（服务器端代理） |
| 托管 | 腾讯云 CloudBase（国内直连、无需魔法） |

---

## 二、技术架构

```
┌─────────────────────────────────────────────────┐
│                    手机浏览器 / 电脑浏览器        │
│  ┌──────────────┐  ┌─────────────────────────┐  │
│  │ index.html   │  │ 浏览器自带 TTS 语音朗读   │  │
│  │ (全部前端逻辑)│  │ GPS 定位(Geolocation)    │  │
│  └──────┬───────┘  └─────────────────────────┘  │
│         │ HTTPS 请求（代理，Key 不出前端）        │
└─────────┼───────────────────────────────────────┘
          ▼
┌─────────────────────────────────────────────────┐
│        腾讯云 CloudBase（环境: guide）            │
│  ┌────────────────┐  ┌──────────────────────┐  │
│  │ 静态网站托管     │  │ 云函数               │  │
│  │ index.html 等   │  │ /deepseek (Nodejs)  │  │
│  │                │  │ /geocode  (Nodejs)   │  │
│  └────────────────┘  └──────────┬───────────┘  │
│                                 ▼               │
│                       DeepSeek API / 高德 API    │
│                       （Key 在云函数环境变量）     │
└─────────────────────────────────────────────────┘
```

**关键设计：API Key 永远放在服务器端（云函数环境变量），前端只通过代理地址调用，任何人 F12 都看不到 Key。**

---

## 三、目录结构

```
景点讲解App/
├── index.html           # ★ 主程序（所有前端功能、数据、页面都在这里）
├── manifest.webmanifest # PWA 清单（App 名称/图标/全屏配置）
├── sw.js               # Service Worker（离线缓存，注意：2026-08 已改为不强制缓存）
├── icons/              # App 图标（144/180/192/512，手机桌面显示用）
├── api/                # Vercel 函数版代理（备用，国内需魔法）
│   ├── deepseek.js
│   └── geocode.js
├── proxy/              # PHP 版代理（备用，给 PHP 主机用）
│   └── deepseek.php
├── functions/          # 云函数本地目录（备用）
├── cloudbase/          # CloudBase CLI 配置目录（备用）
├── cloudbaserc.json    # ★ CloudBase 部署配置（含环境变量，已加入 .gitignore 不入库）
├── vercel.json         # Vercel 配置（备用）
├── README.md           # 自带说明文档
└── .gitignore          # 忽略敏感文件
```

---

## 四、开发环境与工具

| 工具 | 用途 | 是否已装 |
|---|---|---|
| 代码编辑器（VS Code 等） | 修改代码 | 任意文本编辑器即可 |
| Git | 版本管理、推 GitHub | ✅ 已装（2.53.0） |
| TCB CLI（腾讯云开发命令行） | 部署到 CloudBase | ✅ 已装 |
| Node.js | 运行 TCB CLI / 云函数 | ✅ 已装 |
| 浏览器 | 本地调试 | ✅ Chrome |

### 登录 TCB CLI（一次即可，长期有效）

```bash
tcb login
```

> 会弹出浏览器，微信扫码登录腾讯云即可。

---

## 五、本地开发 / 调试方法

1. 直接双击打开 `index.html`（本地文件），大多数功能可测；
2. 真 GPS 定位**必须 HTTPS**，本地文件测不了定位，用「模拟演示」代替；
3. 改完代码保存后，浏览器 **Ctrl + F5 强制刷新**（避开缓存）；
4. 打开浏览器 F12 → Console，看有没有红色报错（排查问题的第一手信息）。

> ⚠️ 注意：本地打开时，AI 讲解需要代理地址或直连 Key；没配 Key 时 AI 会输出「演示讲解」，其余功能正常。

---

## 六、部署：腾讯云 CloudBase（当前正式环境）

### 6.1 环境信息

| 项目 | 值 |
|---|---|
| 环境名 | `guide` |
| 环境 ID | `guide-d4grybdwde2f9d281` |
| 网站地址 | `https://guide-d4grybdwde2f9d281-1432633719.tcloudbaseapp.com` |
| DeepSeek 代理 | `https://guide-d4grybdwde2f9d281-1432633719.ap-shanghai.app.tcloudbase.com/deepseek` |
| 高德代理 | `https://guide-d4grybdwde2f9d281-1432633719.ap-shanghai.app.tcloudbase.com/geocode` |
| 套餐 | 免费体验版（约 6 个月新人期，之后回到每月免费额度，个人自用足够） |
| 区域 | 上海 |

### 6.2 更新云函数（AI 讲解 / 定位识别）

```bash
# 更新 DeepSeek 代理函数（AI 生成讲解）
tcb fn deploy deepseek -e guide-d4grybdwde2f9d281

# 更新高德代理函数（定位识别）
tcb fn deploy geocode -e guide-d4grybdwde2f9d281
```

### 6.3 更新静态文件（改完 index.html 等之后）

```bash
# 更新主程序（最重要，改功能就执行这个）
tcb hosting deploy index.html -e guide-d4grybdwde2f9d281 --concurrency 1

# 更新其他静态文件（按需）
tcb hosting deploy manifest.webmanifest -e guide-d4grybdwde2f9d281
tcb hosting deploy sw.js -e guide-d4grybdwde2f9d281
tcb hosting deploy icons -e guide-d4grybdwde2f9d281
```

### 6.4 云函数环境变量（Key 安全存放处）

配置文件 `cloudbaserc.json`（**已 .gitignore，不会提交到 GitHub**）：

| 变量名 | 用途 | 当前状态 |
|---|---|---|
| `DEEPSEEK_KEY` | DeepSeek API Key | ✅ 已配置 |
| `AMAP_KEY` | 高德「Web服务」Key | ✅ 已配置 |

> 修改 Key 的两种方式：
> 1. 直接编辑 `cloudbaserc.json` → `tcb fn deploy deepseek -e guide-...`
> 2. 或 CloudBase 控制台 → 云函数 → 选择函数 → 配置 → 环境变量 → 修改

### 6.5 注意事项

- **免费体验版不支持绑定自定义域名**（绑定需升级个人版 ¥19.9/月，个人自用建议不花这钱，手机装成 App 后看不到网址）；
- 改完环境变量要**重新部署对应函数**才生效；
- 部署静态文件后，手机端可能因缓存看不到新版 → **强制刷新 / 清除站点数据 / 卸载重装**。

---

## 七、部署：Vercel（备用方案 · 国内需魔法）

> 之前用过，因国内访问不稳定已弃用，保留作为备用。

### 方式 A：GitHub + Vercel 自动部署

1. 推到 GitHub 仓库（`522579173-svg/wangrenmin-guide`）；
2. vercel.com → Add New → Project → Import 该仓库；
3. Framework 选 Other，直接 Deploy；
4. 在 Vercel 项目 Settings → Environment Variables 配置 `DEEPSEEK_KEY`、`AMAP_KEY`；
5. 改环境变量后需重新 Deploy。

### 方式 B：Vercel CLI

```bash
npm i -g vercel
cd 景点讲解App
vercel login
vercel --prod
```

---

## 八、GitHub 版本管理

```bash
# 查看状态
git status

# 提交并推送（注意：Key 永远不会入库，.gitignore 已处理）
git add -A
git commit -m "说明这次改了什么"
git push origin master
```

> ⚠️ 安全红线：
> - **绝不允许**把 `sk-`、`amap key` 写进代码提交；
> - `cloudbaserc.json` 已加入 `.gitignore`；
> - 万一 Key 泄露（GitHub 安全扫描会拦截），去平台**重置 Key** 并重新部署。

---

## 九、内容维护：怎么加景区 / 讲解点

打开 `index.html`，找两个数据区：

### 9.1 普通景点列表 `APP_DATA`（首页城市浏览）

格式（照故宫示例）：
```js
{
  city: "北京",
  spots: [
    { name: "故宫博物院", intro: "简介...", script: "讲解词..." }
  ]
}
```

### 9.2 仿真人导游景区 `GUIDE_AREAS`（手绘地图 + 自动讲解）

格式要点（照西湖/灵隐寺示例）：
```js
{
  name: "景区名",
  map: { w: 1000, h: 600, bg: "...", svg: "...", elements: [手绘地形] },
  points: [
    { name: "讲解点名", x: 200, y: 300, r: 40, script: "讲解词..." }
  ]
}
```

### 9.3 自动生成景区（推荐，不用手动写地图）

- 首页 →「🎙️ 仿真人导游」→「✨ 自动生成新景区」→ 输入景区名；
- 高德自动搜 POI + DeepSeek 批量生成讲解 + 自动画地图；
- 生成结果存在手机 localStorage，**只在本机有效**（换手机需重新生成）。

---

## 十、核心逻辑：三级自动补全

```
GPS 坐标 → 逆地理编码（高德/OSM）→ 地点名
  → ① 内置库（APP_DATA / GUIDE_AREAS）命中 → 直接播放精品讲解
  → ② 我的讲解库（localStorage）命中 → 直接播放（距离<120米自动开讲）
  → ③ 都没命中 → DeepSeek AI 当场生成 → TTS 朗读 → 存入讲解库
```

**效果：陌生景区第一次 AI 当场讲，第二次 GPS 一到自动开讲，越用越省。**

---

## 十一、语音播放：暂停/继续的实现（重要）

> 2026-08 重写为「**切片播放**」架构，原因：浏览器 `speechSynthesis.pause()/resume()` 在 iOS/部分浏览器不可靠（暂停后语音仍继续、继续从头播）。

**原理**：讲解词按标点切成句子 → 逐句朗读 → 暂停时停掉当前句并记住「读到第几句」→ 继续时从下一句开始。

```
讲解词 → 按 .。！!?？；; 切句
暂停 → 停止当前句，记录 currentIndex
继续 → 从 currentIndex 的下一句开始（不从头）
语速切换 → 当前句用新语速重播
```

> 改播放逻辑时，务必保持这个「切片 + 状态机」架构，不要退回用 pause()/resume()。

---

## 十二、常见问题排查（FAQ）

| 现象 | 原因 | 解决 |
|---|---|---|
| 页面空白只剩 `<div id="app">` | JS 脚本执行中断（或旧缓存） | ① Ctrl+F5 强刷 ② F12 Console 看红色报错 ③ 清除站点数据/卸载重装 |
| 手机看不到新版 | Service Worker / 浏览器缓存 | 强刷 / 清缓存 / 卸载重装；线上 sw.js 已改为不强制缓存 |
| 暂停后语音还在播 | 用了旧版 pause() 逻辑 | 用「切片播放」架构（见第十一节） |
| AI 生成转圈/失败 | Key 错误 / 代理地址不对 | 检查设置页代理地址；控制台看报错 |
| 定位不准 | 没配高德 Key / 非 HTTPS | 配高德 Key；必须 HTTPS 环境 |
| 本地打不开定位 | 本地文件非 HTTPS | 用「模拟演示」；部署后手机测试 |
| 电脑能开手机打不开 | 手机网络/缓存 | 换流量试 / 清缓存 |
| 点自动生成景区地图出不来/点了没反应 | 旧版生成的景区数据缺字段（2026-08-12 已修） | 刷新页面后重开；旧数据打开时会自动补齐字段并存回 |
| 演示点了没反应/小人乱跳 | 自动GPS劫持演示（2026-08-12 已修） | 刷新页面后重试 |
| 点小房子讲错景点（点A讲B） | 高德推荐度排序/容差过大（2026-08-12 已修） | 刷新页面后重试；有歧义时会弹「附近景点」列表，选一下即可 |

---

## 十三、成本与限额

| 项目 | 费用 |
|---|---|
| CloudBase 托管 | ¥0（免费体验版） |
| DeepSeek | ≈¥0（一条讲解约 1k token，个人每月几分钱） |
| 高德 | ¥0（免费额度内） |
| TTS 语音 | ¥0（浏览器自带） |

> 免费体验版 6 个月后回到每月免费额度，个人自用几乎不会触发计费。

---

## 十四、优化记录（2026-08-09 大优化）

> 本轮共 10 项改进，全部已实测/已部署（见下）。

| # | 改动 | 说明 |
|---|---|---|
| 1 | TTS 白屏保护 | 浏览器无 speechSynthesis 时不再抛错白屏，改文字展示讲解 + 提示 |
| 2 | 数字转中文朗读 | 讲解词中「300」「1406」等阿拉伯数字自动读成「三百」「一千四百零六」 |
| 3 | 高德限流退避 | 逆地理/POI 接口并发限流（最多2在途）+ 10分钟内存缓存，防免费档超频 |
| 4 | 代理地址 HTTPS 校验 | 设置页保存时校验必须以 https:// 开头（http 会被浏览器拦截） |
| 5 | iOS 滚动修复 | 页面切换用 window.scrollTo + documentElement.scrollTop 双保险（微信内不失效） |
| 6 | 播放进度 + 上下句 | 播放器显示「第 N / 共 M 句」，新增 ⏮⏭ 按钮（对标 audioguide-app） |
| 7 | GPS 防重触发 | 同一讲解点 3 分钟冷却，防止 GPS 抖动反复开讲（对标 GeoAR） |
| 8 | 深浅色模式 | 设置页 🌗自动/☀️浅色/🌙深色，默认跟随系统（对标 audioguide-app） |
| 9 | iOS 定位提示 | 定位失败给出「去设置开权限/开GPS」等具体指引 |
| 10 | 自动生成景区优化 | detectCity 增加城市探测兜底、POI 搜索超时延长、去重更稳 |
| 11 | 自由导览详情统一 | GPS/AI 识别结果生成后统一改为内置5城15景的「景点简介/讲解词/小贴士」三卡片样式，并共用底部播放器 |
| 12 | 搜索四层兜底 | 首页搜索扩展为城市→5城15景→导游地图/讲解点→AI智能导览；未收录地点（如夫子庙）自动进入AI生成，不再只报“没找到” |
| 13 | GPS陌生地点自动生成 | GPS识别到陌生地点后自动把地点名称、地址、经纬度发到授权的CloudBase DeepSeek代理生成讲解；同地点10分钟最多尝试一次，失败保留手动重试 |
| 14 | 自动地图稳定性 | 过滤酒店/地铁/市场/售票处等无关POI；小景区2个有效点也可生成；完成后立即打开；同名地图覆盖旧版防重复 |
| 15 | GPS自动生成导游地图 | GPS识别陌生景区后，并行生成当前地点讲解和整个景区导游地图；地图生成函数直接接收识别名称、不依赖输入框；自动保存到仿真人导游列表，同景区30分钟防重复 |
| 16 | 旧缓存补建与单点地图 | 命中旧讲解缓存后不再提前返回，仍检查并补建地图；桥梁/小景点即使高德只有1个或0个有效POI，也用GPS当前位置生成单点地图；手动页新增“用当前位置自动生成” |
| 17 | iPhone安装修复 | 恢复Service Worker注册并升级v4缓存；补齐PWA id/scope/start_url/display_override、180图标声明；iOS首页常显安装按钮并提供Safari三步引导，桌面模式自动隐藏 |
| 18 | 普通话语音修复 | TTS语音选择严格优先zh-CN普通话，排除粤语(zh-HK)/台语(zh-TW)/澳门/新加坡等方言口音；无普通话时交还系统按zh-CN匹配，杜绝苹果读成粤语 |
| 19 | 顶部返回入口修复 | 首页不再隐藏顶部入口，显示“‹ 首页”作为明确导航；内页显示“‹ 返回”；按钮增加背景、边框和安全区适配，避免白色文字融入顶部或被iPhone状态栏遮挡 |
| 20 | 地图缩放+定位导航线 | 景区地图支持放大缩小（右上角＋/－/⟳按钮、滚轮、双指捏合、拖拽平移，1~8倍）；放大超过2.5倍时显示「当前定位→下一景点」红色流动导航线（含箭头和距离米数），下一景点=最近未听完的讲解点，听完自动切换下一个，全部听完自动消失；打开地图/重置时自动回到全景 |
| 21 | 真实地图替换手绘地图 | 景区地图改用 Leaflet + 高德瓦片（真实地图，本地引入 lib/leaflet.js，无需key）；景点用 emoji+名称标记标在真实经纬度上（内置景区由0-100坐标反算经纬度，auto景区直接用POI经纬度），点击标记立即开讲（点到再解说），听完变绿打对勾；游览路线画成真实地图折线；游客位置实时显示在真实地图上；Leaflet自带缩放/双指/拖拽；放大到16级显示「定位→下一景点」导航线；手绘SVG地图与自定义缩放控件已移除 |
| 22 | 实时定位+点到即讲 | ①打开景区地图自动尝试GPS实时定位（无权限/不支持自动回演示模式）；GPS模式下地图自动跟随游客位置（走出视野才平移，不晃眼），右上角「📍 跟随中」按钮可开关；②点到再解说升级为「点到景点位置即讲解」：点击距景点120米内任意位置（不限于图标）直接开讲，多景点重叠区自动取最近，点到远处空地仍按点图模式移动 |
| 23 | 定位按钮+景点链接+动画修复 | ①「📍 定位」按钮：点一下地图跳回你实际所在的位置——在景区内则换算进导览坐标、红色游客点居中、走到哪讲到哪；在景区外（如人在南京看故宫地图）地图直接跳到真实位置并显示蓝色波纹「我的位置」标记+逆地理编码显示地名；②景点标记超链接化：鼠标移上名称变蓝加下划线、图标放大，悬停提示「点击听XX讲解」，点击立即开讲；③修复地图动画残留bug：连续定位/缩放打断动画会导致图层transform残留错位，统一禁用zoom/fade动画、程序化视图移动改animate:false |
| 35 | 点击底图小房子触发讲解 | 不叠加标签/热区（保留底图原样）：点击地图任意处 → 取经纬度 → 高德周边搜索（半径200米/风景名胜|历史遗迹）→ 过滤噪音 → 命中最近POI → 讲解库直接播/未命中DeepSeek生成存库；原marker点击讲解不变（spotNear容差从120米缩至80米，避免误匹配更远的已知景点）；拖拽由Leaflet自动屏蔽不误触；无hover/手型反馈；geocode云函数③周边搜索分支重新加入并部署；实测：点嘉量位置播放嘉量、点九龙壁播放九龙壁、讲解库命中直接播 |
| 36 | 点不准小房子优化（容差+翻页+名字匹配） | 问题：底图小房子图标小、鼠标点偏（30-100米）就反查落空，且高德around接口按推荐度排序（非距离）一页最多25条，目标POI常被挤出前20条。GitHub调研参考：Leaflet.AlmostOver（"鼠标几乎在图层上"即触发）、leaflet-clicktolerance（Path点击容差）、leaflet-geoman snapping（吸附最近）→ 翻译为地理距离容差+吸附最近+命中反馈。实现：①反查半径200→500米、offset 10→20、新增page翻页透传（geocode云函数+cloudbase备份同步部署），前端翻3页拉全（约60条）再统一按距离排序——点偏100米也能在page2命中乾清宫；②新增matchSpotByName：反查POI名去"故宫博物院-"前缀后与本地景点名全等/互相包含即匹配，命中直接spotTap播本地精品讲解（快且支持版本切换/足迹），未匹配才AI生成；③点击立即toast「🔍 正在识别附近景点…」，落空明确提示「附近没找到可讲解的景点」不再静默；④spotNear本地容差保持80米不变（放大会误命中更远已知景点，如点嘉量小房子误命中87米外乾清宫）；⑤实测：点准乾清宫page1命中、点偏100米page2命中、点午门page1第1名、点九龙壁讲解库命中直接播、点故宫外中山公园区域正常生成讲解；⑥发现：内置0-100坐标反算经纬度与真实POI有偏差（如乾清宫差约300米），marker显示位置与底图小房子不完全重合——靠反查翻页+名字匹配兜底，不改内置坐标 |
| 37 | 真机静音修复+提示完善 | 老王反馈「网页上没看到效果」，排查：①线上文件与云函数均最新、CDP线上真实点击链路完整（点击乾清宫→播放器第1/3句出声）→ 客户端缓存旧版为主因（SW/浏览器缓存，需强刷/清站点数据/卸载重装）；②发现真bug：点击→异步反查1-3秒→回调里speak，已脱离浏览器「用户激活」窗口，iOS/Chrome会静音拦截且onerror被静默跳过（无任何反馈）→ 新增ttsUnlock()在点击手势内resume+speak空句解锁，后续异步朗读保持可出声；③speakSentences连续2句失败（ttsFailCnt）toast「语音被浏览器拦截未出声，点底部播放键可手动播放」不再静默；④poiHitClick无代理/网络失败分支加明确toast提示；⑤排查中还发现：地图打开后前几秒fitArea/invalidateSize时序导致中心/zoom变化，取屏幕坐标点击需等地图稳定（2-3秒）后再点，否则会点偏 |
| 49 | 自驾手机端跑通（对标旅途随身听）+ iOS 防锁屏 | 老王确认产品方向对标「旅途随身听」（驰笙科技电子导游App：走到哪讲到哪、会员收费）——我们已有其核心体验（仿真人导游）且多两块（跨城自驾导航、DeepSeek AI 陌生景区生成），免费自用。本轮修复手机端关键坑：**iOS Safari 不支持 navigator.wakeLock**（仅安卓Chrome）→ iPhone 行驶中屏幕自动锁→PWA冻结→GPS/讲解全停。修复：①requestWakeLock 增强——wakeLock 可用走 wakeLock、不可用/请求失败/同步异常一律 fallback `noSleepStart()`；②noSleepStart：WebAudio 循环播放音量 0.0001 的白噪声（人耳听不见、音频会话活跃，iOS 上"播放中的页面"不锁屏，NoSleep.js 同原理），用户手势内启动（startDrive 点击触发，满足自动播放策略）；③noSleepStop：stopDriveAll 退出时停音频+关 context；④开始自驾 toast「请保持 App 在前台、连车载蓝牙、音量调大（锁屏/切后台会中断导航）」；⑤visibilitychange 回前台提醒「已回到自驾导航」。实测（CDP 模拟 iPhone/安卓双场景）：iPhone（wakeLock=undefined）→静音音频启动循环✅；安卓（request 成功）→wakeLock 拿锁、不起音频✅；完整流程（规划→开始→行驶触发讲解 1/15→退出）→全屏/面板/退出清理（音频停+GPS停）✅。真机自测清单：①定位授权②规划③开始自驾④观察屏幕常亮⑤转弯播报⑥景点讲解⑦面板跟随 |
| 48 | 自驾板块全链路排查（6项修复） | 老王要求全面排查自驾导航板块。①**触发逻辑缺陷（重要）**：讲解点是采样点周边6km搜的，车在高速上到不了景点门口（直线>触发半径800m），大部分讲解点永远不触发（实测17点只触发2-3个）→预计算每个讲解点在路线上的投影 index（routeIdx，buildDriveSpots 尾部扫全路线），driveGPS 维护当前最近路线点 index（dvCarIdx，drawDriveProgress 更新）→触发条件=直线≤半径 或 routeIdx≤carIdx+3（**车经过景点正对路段即讲**，高速场景核心修复）；②**灰线图层泄漏**：drawDriveProgress 每10秒重绘都 addTo 新灰线、旧灰线不删（dvRouteLine 只指向橙线）→新增 dvPassedLine 管理灰线，drawDriveRoute/drawDriveProgress 两处统一清理（**排查中发现 drawDriveProgress 曾有两份定义，旧版覆盖新版导致修复失效，已删旧版**）；③**到达提示缺失**→navSteps 全播完且距终点≤1km 播报「前方一千米，即将到达目的地XX」+面板🏁；④**行程去重**：saveDrive 同 from→to 覆盖旧记录（原越存越多）；⑤**空景点提示**：dvSpots 空时面板显示「沿途暂未筛出景点」；⑥**历史行程提示**：载入无讲解点的旧行程时提示可重新规划。实测：北京→天津 mock 行驶——5/9 按路线顺序触发（明城墙→乾坤殿→平山堂→万新清真寺→文明交通主题公园）、carIdx 更新、灰线分段、讲解后导航播报接上、navDone 14/25。注：mock 60倍速下剩余未触发点是窗口假象（真实车速 carIdx+3≈300m 窗口 9-12 秒足够） |
| 47 | 导航+景点解说链路排查修复（6处） | 老王要求排查自驾"导航和景点解说"是否跑通。通读+实测发现并修复：①**iOS语音拦截**——导航播报/讲解都在GPS异步回调里speak，脱离用户手势窗口会被静音拦截→startDrive手势内补 ttsUnlock()（空句解锁）；②**转弯播报丢失**——讲解播放中 speakNav 直接return且已标记 done（丢转弯）→改为"实际播了才标记"（speakNav 返回 true/false，ttsState+讲解状态检查），讲解中未播不标记可补播；③**讲解结束补播转弯**——playGuideScript 新增可选 onEnd 参数（向后兼容），playDriveSpot 传 driveNavCatchUp（距未播转弯≤800米补播）；④**退出停播**——stopDriveAll 补 stopSpeak（退出自驾/返回时停掉讲解）；⑤**全屏布局**——invalidateSize 60ms→200ms+600ms 兜底；⑥**dvLat/dvLng 未声明**（driveNavCatchUp 依赖）补声明+driveGPS 赋值。实测北京→天津 mock 行驶：ttsUnlock空句→4条连续转向播报（东二环/光明桥/东三环/分钟寺G2京沪）→平山堂讲解接上→面板🚧指令/下一站/已过1/10→navDone 10/10→地图跟随到天津→全屏正常。讲解与导航共存：讲解播放中导航跳过、讲完补播 |
| 46 | 自驾内置导航增强 + CarPlay 调研 | 老王需求：点「开始自驾」=内置导航（不用另开高德导航），导航基础上加景点解说；调研 CarPlay。①**CarPlay 调研结论**：CarPlay 只支持 App Store 原生 iOS App、不支持 PWA/网页应用（平台硬性限制），且需向 Apple 申请 CarPlay entitlements（参考 Organic Maps issue#6184）——本 App 是 PWA，**当前不可行**，未来需做原生 iOS App 才可能接入；替代=手机端完整导航体验。②**云函数**：direction 模式 extensions=base→all，新增 steps（每步中文转向指示 instruction + 起点坐标），供前端播报。③**前端导航增强**：地图跟随（GPS 回调 panTo 5秒节流）、已走/未走路线分段（灰色/橙色，10秒重绘）、**导航转向语音播报**（距下一 navStep≤500米且未播过→TTS「前方，{instruction}」，数字自动转中文；景点讲解播放中不播导航，导游优先）、驾驶面板升级（下一站大字+当前导航指令🚧+已过N/M）、**全屏驾驶模式**（body.drive-mode：隐藏表单/列表/顶栏，地图铺满+底部面板浮层，WakeLock 常亮，退出自驾按钮恢复）。实测：北京→天津 mock 行驶——地图中心跟随、播报「前方，沿东二环入口…右转」、面板显示「🚧向西北行驶103米到达目的地」、景点讲解（乾坤殿/平山堂）与导航播报共存、已过2/18。历史行程兼容：老行程无 navSteps 则只讲解不播报 |
| 45 | 跨城自驾游功能（GitHub调研+需求实现） | 老王要求调研GitHub自驾方案并增加功能。调研：GitHub自驾项目均为「行程规划器」（MyTripPlanner AI规划/RoadRunner路线+预算/GPX画线导出/微信小程序自驾=电商式），**无「自驾+沿途景点自动语音讲解」先例**——本App的GPS触发讲解是独有优势；高德驾车路线API（v3/direction/driving）免费额度100万次/日。实现：①geocode云函数新增④驾车路线模式（origin/destination/waypoints→distance/duration/polyline折线，strategy=10综合最优，行functions/geocode/index.js）；②前端自驾模块（page-drive）：起点（可用当前位置）→途经点(≤3,可不填)→终点，关键词→高德POI坐标解析，路线折线绘制（Leaflet复用），沿途讲解点=路线每8km采样→周边6km搜索(types=110000风景名胜大类)→fameScore名气评分筛选（类型权重+著名后缀白名单+噪音词黑名单降权，阈值45，宁缺毋滥）+500m相邻去重(只留名气最高)→按起点距离排序**不设数量上限，筛出多少就是多少**；③自驾模式：GPS跟随+按车速动态触发半径(60km/h≈400m，100km/h≈600m，speed*3.6*6，400-800m)+5分钟冷却→到点自动开讲（讲解库命中直接播/DeepSeek生成存库）；④驾驶面板：下一站名称/距离/已过数；⑤WakeLock屏幕常亮（手机锁屏断GPS）；⑥行程持久化spotguide_drives（最近10条，含路线+讲解点，可重开）；⑦入口：2026-08-14 移动到首页，做成与「我的足迹」同款 free-entry 卡片（位于足迹卡后，首页 id=driveEntry），仿真人导游列表不再放自驾卡片，自驾页返回改回首页。实测：北京→廊坊→天津 152km/149分钟/1603路线点/20讲解点（明城墙遗址公园/崇文门等真实景点）、mock GPS沿路线行驶自动触发3个讲解点并播放、回归（故宫/点图/城市/足迹）全过。边界：不是实时导航（无逐向语音），是自驾游向导；途经点≤3 |
| 44 | 首页按城市浏览：增删城市 | 老王需求实现：①文案动态统计「共X城·Y景」（X=用户列表城市数、Y=各城景点累加）；②城市卡片右上角✕删除 → 自定义确认弹窗（提示文案+取消/确认移除）→ 移除+统计刷新+持久化（localStorage `spotguide_cities` 存城市id数组，未配置默认全部城市）；③「+ 添加城市」虚线卡片（同网格同尺寸）→ 选择弹窗列出软件全部城市、支持搜索定位、已添加置灰不可重复选、单选添加；④搜索模式与用户模块共存：搜索命中显示「匹配X城·Y景」且不带增删按钮，清空搜索恢复用户模块。代码：renderCityModule/getUserCities/renderCityPicker/CITY_KEY（~812行）、CSS .city-del/.city-add/.cp-item。实测：初始默认5城15景→删上海(4城12景)→取消删除保留→添加上海回5城15景→已添加置灰→搜索定位→刷新持久化，全过。注：软件城市数据仅5城，弹窗列表=软件支持城市，加新城市需先扩展APP_DATA |
| 43 | 演示改为开关式按钮 | 「🚶 演示」与「跟随中」「GPS」一致：点一下开启（自动沿路线走、到点自动开讲），再点一下关闭（停止+状态栏「演示已关闭」）；底部「▶ 开始游览」大按钮保留可暂停/继续，两入口状态同步；实测开→走→关→停→再开全部正常（toggleDemo） |
| 42 | 自动生成景区地图出不来修复（旧数据兼容） | 老王反馈「自动生成景区点了没反应」→ CDP复现：旧版生成的auto景区数据缺 x/y/r/id/script 字段，打开时 buildBestRoute 读 s.x 为 null 崩、地图出不来且无任何提示。修复：①ensureAreaData() 数据自愈（按经纬度反算x/y、补r/emoji/id/script、route兜底），loadAutoAreas 读入即自愈并持久化 + openMap 打开前再自愈；②buildBestRoute 过滤连续重合点（游客位置与景点重合时Leaflet折线退化崩）；③updateBestRoute/updateGuideLine 折线创建 try-catch 保护（刚建图时renderer未就绪画不上就跳过，fitArea后的zoomend会重画）。实测：旧格式数据→刷新→列表点开→地图12瓦片正常，故宫/点图/演示无回归 |
| 41 | 6个工具栏功能全面排查（8处修复+2优化） | ①locateMe景区外提示「[object Object]」→ 用 name.address；②GPS定位开关被上轮误伤（watchPosition只认gMode==="gps"，定位开关模式全废）→ 条件改 `gMode!=="gps" && !gLocating`；③toggleGPS退出跟随恢复gStart按钮；④setMode切demo/tap时 gLocating=false 防高亮残留；⑤setMode("gps")时 gLocating=false 防双开双亮；⑥locateMe景区内补 checkTrigger 自动开讲；⑦drawMap清理myLocMarker防蓝点残留；⑧openAsk先stopGuideSpeak防两个声音打架 + askGuide空问题/busy提示；优化：演示步速5.5→9单位/秒、演示间隙状态栏显示「🚶 正在前往「XX」…」。全部CDP实测通过 |
| 40 | 演示模式「没作用」修复（GPS劫持） | 老王反馈「演示没作用」→ CDP复现：openMap的400ms自动GPS定时器无条件劫持——进图立即点「🚶 演示」+「▶ 开始」，400ms后GPS成功把游客位置跳到右下角、演示停摆。修复：①gUserMode标志（setMode置true，用户手动选过模式后自动GPS不再切）；②watchPosition回调加 `gMode!=="gps" && !gLocating` 双保险；③GPS失败回退恢复「▶ 开始游览」按钮。实测：演示自动走→午门→太和殿逐景点开讲 |
| 39 | 点不准彻底根治（距离排序+歧义列表+容差收紧） | 老王反馈「还是点不准」→ 线上实测实证：高德around默认按**推荐度**排序非距离（太和殿坐标反查189条，太和殿本体不在前25名），翻3页赌推荐度拿不到想点的POI。修复三层：①后端geocode云函数 location模式强制 `sortrule=distance`（functions/geocode/index.js），前端改单次请求(offset25不再翻页)；②前端poiHitClick：无歧义（最近≤60米且第二近至少远40米）直接播，有歧义弹「附近景点」候选列表（点击位置气泡、按距离排序带米数、最多6条）让用户点选——反查本质是猜，列表让用户眼睛看到哪个选哪个，100%不会点A讲B；点击即时显示「🔍正在识别…」探针；③本地标记链路：SPOT_TAP_DIST 80→25米（80米会误命中，如点太和门小房子33米外误播太和殿）、触发圈circle去掉直接spotTap改冒泡到地图统一走反查（70米圈会吞掉圈内小房子点击）、map click不再调checkTrigger（70米触发圈会点A讲B——点太和门误触发太和殿圈）。实测6场景全过：太和门→太和门、保和殿西侧→弹列表、太和殿marker→太和殿、体仁阁→体仁阁、空地→弹附近列表、demo走到午门→自动播午门 |
| 38 | 点A讲B修复（匹配收紧+翻页提速） | 老王反馈「还是点不准」→ CDP复现：点体仁阁位置播放了「太和殿 · 讲解」。根因①：matchSpotByName用「互相包含」匹配——反查POI「太和殿日晷」(102米)名字包含本地景点名「太和殿」→ 点体仁阁(0米,本地无marker)误命中190米外的太和殿marker；根因②：3页串行拉完才统一处理，等待过长。修复：①匹配规则收紧为「去"故宫博物院-"前缀后完全相等」才命中（"太和殿日晷"≠"太和殿"），不完全相等走poiPlay AI生成兜底；②附属物POI过滤：日晷/牌匾/铜鹤/铜龟/石狮/石拱/栏杆/屋檐/脊兽/石阶/彩纹/红墙/橙瓦/白墙/雕塑/建筑群等观景附属物不是独立讲解目标（会干扰匹配）；③匹配本地景点要求POI距离≤150米（超过跳过——点A讲B根因之一）；④翻页策略改为「本页已有<120米有效POI立即处理，否则继续翻页」——真实点击1-2秒出结果，点偏才翻页兜底。实测全部通过：点体仁阁→讲体仁阁(4秒生成入库)、点准乾清宫/午门→本地精品讲解、点偏100米(实际近麟趾门50米)→讲麟趾门（严格点哪讲哪）、点嘉量→讲嘉量、太和殿日晷不再误匹配 |
| 34 | 撤销地图文字POI功能 | 老王决定撤销第33条「地图文字POI可点击讲解」：前端POI文字标签层（.poi-label样式/drawPoiLabels/poiTap/drawMap调用）已全部移除；geocode云函数③周边搜索分支已还原并重新部署；实测：无POI标签残留、原marker讲解/地图功能正常 |
| 33 | 地图文字POI可点击讲解 | 高德底图上的景点文字（内库/中左门/贞度门等）叠加可点击文字标签层：①geocode云函数新增③周边搜索模式（location/radius/types=风景名胜|历史遗迹）；②进景区自动周边搜索并过滤噪音（打卡点/售票处/餐厅等），叠加白底圆角文字标签（最多15个，层级高于marker避免被盖）；③PC hover变蓝下划线+手型，手机点击即触发：讲解库命中直接播，未命中DeepSeek按需生成并存讲解库（下次直接开讲）；④防拖拽误触：Leaflet marker自带拖拽不触发点击；实测12个标签加载/hover变色/点击生成讲解/存库/原marker回归全部正常 |
| 32 | 跟随中改为开关 | 「跟随中」改为开关按钮：点一下开启 GPS 跟随（定位+地图跟走+自动讲解），再点一下关闭（停止定位回到演示模式）；按钮高亮=跟随开启中；关闭/开启时状态文案同步（跟随已关闭/正在开启跟随）；实测开关循环开→关→开→关全部正常 |
| 31 | 恢复 GPS 按钮 | 工具栏恢复第6个按钮「📍 GPS」（顺序：跟随中→问导游→定位→点图→演示→GPS）；功能=GPS定位开关：开启后实时定位显示位置但地图不自动跟随（可自由拖动），与「跟随中」（定位+地图跟走）互补；开启时若在跟随中模式自动退出；状态文案区分（已定位·地图可自由拖动）；实测开启高亮/关闭复位/不跟随均正常 |
| 30 | 工具栏按钮顺序调整 | 地图右上角工具栏合并为统一 #mapBar，按钮顺序改为：跟随中→问导游→定位→点图→演示（原为演示→点图→跟随中→问导游→定位）；样式/交互/状态逻辑不变；定位逻辑抽成 locateMe 函数复用；实测顺序与各按钮功能（弹层/模式切换/高亮跟随）全部正常 |
| 29 | 修复自动生成后地图打不开（_leaflet_events） | 报错场景：自动生成景区（如卧龙湖大桥）→ AI讲解词生成成功 → 打开地图时 Leaflet 初始化失败（Cannot read properties of undefined _leaflet_events）。根因：openMap 里 drawMap（初始化Leaflet）在 go(map)（显示页面）之前执行，隐藏容器(0尺寸)上初始化地图导致实例异常。修复：①openMap 改为先 go(map) 显示页面再 drawMap 初始化（容器有实际尺寸）；②drawMap 初始化前检查 #realMap 容器存在，不存在直接return；③L.map 初始化包 try-catch，失败 toast 提示不白屏；④足迹页 fpMap 同样加容器检查+try-catch；实测故宫与 auto 景区（卧龙湖大桥）打开均正常、console 零报错 |
| 28 | 跟随模式修复完善 | 排查发现上次删跟随按钮时误删了「地图自动跟随」核心逻辑，已恢复：GPS跟随模式下地图自动跟着游客位置走（走出视野才平移，人永远在屏幕里）；模式按钮改名「📍 跟随中」贴合用户认知；其余链路确认完好（自动定位/走到景点自动讲解/已听进度/重置按钮/「已定位，正在跟随」状态文字）；实测地图跟随精确到0.00001度 |
| 27 | 地图讲解接入底部播放器 | 景区地图讲解从气泡朗读升级为完整底部播放器（与详情页同款）：切片播放显示进度（第N/M句）、暂停/继续、上一句/下一句、0.8x/1.0x/1.2x语速；讲解气泡保留（显示文字+版本切换）；播放器播完自动结束讲解状态（关气泡、演示模式恢复）；讲解结束后可点播放键重听（gLastSpot）；问导游回答也走播放器朗读；注：TTS需真实用户手势（Chrome自动播放策略），脚本合成点击不触发朗读 |
| 26 | 四大功能优化（GitHub调研） | ①讲解多版本：讲解气泡内📖标准/👦儿童/🎓深度一键切换，儿童/深度版用DeepSeek按需生成并缓存localStorage（离线可切）；②AI问导游：地图右上角🤖按钮，弹层提问DeepSeek以现场导游口吻200字内解答+自动朗读，含快捷问题；③离线地图：Service Worker缓存高德瓦片（cache-first，上限1500张超出删最旧1/3），景区没信号也能看图，Safari自动降级；④足迹打卡+路线优化：每次讲解自动记录（localStorage上限500条），首页「👣我的足迹」页含统计（景区/次数/时长）+足迹地图（Leaflet金色标记）+时间线；放大后显示从当前位置贪心串联所有未听景点的橙色最优路线，每听完一个自动重算 |
| 25 | 移除跟随功能 | 「📍 跟随中」按钮及地图自动跟随逻辑已移除（老王决定）；「📍 定位」按钮保留（点一下跳回实际位置）；删除代码：gFollow变量、drawVisitor里panTo跟随、setMode按钮显隐、跟随按钮HTML与off样式 |
| 24 | 景点超链接完善 | 景点名称**默认就是蓝色下划线链接样式**（手机上一眼可点，不再依赖hover）；鼠标悬停：名称浅蓝背景高亮+图标放大1.18倍；按下：图标缩小反馈；点击：图标波纹弹跳动画（spotTap波纹）确认点中+立即开讲；已听过的景点保持绿色下划线链接样式；实测桌面hover/手机触摸/点击开讲全部生效 |

> 后续可选升级方向（备忘，可继续做）：
- [ ] 讲解「当前句高亮」（用 SpeechSynthesisUtterance.onboundary，Chrome/Edge 支持）
- [ ] 多语言 i18n（英文版）
- [ ] 讲解库管理界面（编辑/删除 AI 生成的讲解）
- [ ] 自动生成地图速度优化（异步生成讲解词，地图秒开）
- [ ] 扩充内置景区数据（5A 景区批量）
- [ ] 多语言/方言 TTS
- [ ] 苹果手机安装教学完善
- [ ] UI 美化

---

## 十五、常用命令速查

```bash
# 部署主程序（改完 index.html 后）
tcb hosting deploy index.html -e guide-d4grybdwde2f9d281 --concurrency 1

# 部署云函数
tcb fn deploy deepseek -e guide-d4grybdwde2f9d281
tcb fn deploy geocode -e guide-d4grybdwde2f9d281

# 登录
tcb login

# Git 推送
git add -A && git commit -m "update" && git push origin master
```

---

## 十六、功能总览速查表（2026-08-13 整理，便于调取/修复）

> 代码位置 = index.html 中的函数名/变量名（行号随版本漂移，按名称搜索最稳）。

### 16.1 地图工具栏 6 个功能

| 按钮 | 功能 | 关键代码 | 备注 |
|---|---|---|---|
| 📍 跟随中 | GPS 跟随开关：定位 + 地图跟走 + 到点自动讲 | `toggleFollow` / `setMode("gps")` / `startGPS` | 点一下开、再点一下关；按钮高亮=开启中 |
| 🤖 问导游 | AI 问答 + 自动朗读（DeepSeek） | `openAsk` / `askGuide` / `deepseekChat` | 打开时自动停止当前讲解（防声音打架）；空问题/busy 有提示 |
| 📍 定位 | 地图跳回你实际所在位置 | `locateMe` | 景区内：换算坐标+走到哪讲到哪(checkTrigger)；景区外：地图跳过去+蓝点标记+逆地理显示地名 |
| 👆 点图 | 指哪讲哪：点景点直接讲 | `setMode("tap")` + `spotNear` + `poiHitClick` | 距本地标记 25 米内直接讲；底图小房子走反查识别 |
| 🚶 演示 | 演示开关：自动沿路线走、到点自动开讲 | `toggleDemo` / `gTick` / `checkTrigger` | 点一下开、再点关；步速 9 单位/秒；间隙显示「正在前往「XX」…」 |
| 📍 GPS | GPS 定位开关：只定位显示位置、地图不自动跟随 | `toggleGPS` / `gLocating` | 与跟随中互补；可自由拖动地图 |

### 16.2 点击识别链路（「点不准」相关，最易出问题）

```
点击地图 → spotNear(25米容差，命中本地标记直接播)
        → 未命中 → poiHitClick 反查：
           高德周边搜索（geocode 云函数 location 模式，强制 sortrule=distance 按距离排序，500米/前25条）
           → 过滤噪音/附属物（打卡点/日晷/牌匾/餐厅/服务设施等）
           → 无歧义（最近≤60米 且 第二近至少远40米）→ 直接播（匹配本地名→spotTap，否则 poiPlay AI生成）
           → 有歧义 → 弹「附近景点」候选列表（按距离排序带米数，最多6条）→ 用户点选开讲
```

关键常量/函数：`SPOT_TAP_DIST=25`（本地标记容差）、`poiHitClick`、`matchSpotByName`（完全相等匹配）、`poiPlay`、`showPoiPicker/closePoiPicker`（候选列表）、`showPoiProbe`（🔍识别中探针）、`ttsUnlock`（手势内解锁语音防静音）、`poiBusy`（反查进行中标志）。

### 16.3 数据存储（localStorage）

| Key | 内容 | 备注 |
|---|---|---|
| `spotguide_settings` | 设置（DeepSeek Key/高德 Key/两个代理地址/主题） | 设置页保存 |
| `spotguide_lib` | 我的讲解库（AI 生成的讲解，命中直接播） | 上限自动管理 |
| `spotguide_auto_areas` | 自动生成景区列表 | **读入自动自愈旧格式**（ensureAreaData）并写回 |
| `spotguide_scripts_v2` | 儿童/深度版讲解缓存 | 按需生成 |
| `spotguide_footprints` | 足迹记录 | 上限 500 条 |
| `spotguide_theme` | 主题 auto/light/dark | |

### 16.4 状态变量（调试排查用）

| 变量 | 含义 |
|---|---|
| `gMode` | 当前模式：demo / tap / gps |
| `gLocating` | GPS 定位开关（📍 GPS 按钮） |
| `gUserMode` | 用户手动选过模式（防 openMap 的 400ms 自动 GPS 劫持） |
| `gPlaying` / `gPaused` | 演示状态（开关/播放） |
| `gCurSpot` / `gLastSpot` | 当前讲解景点 / 最后讲解景点 |
| `gDone` | 已听景点集合（标记变绿） |
| `gSpotCooldown` / `SPOT_COOLDOWN_MS` | 同景点 3 分钟防重触发 |
| `currentArea` | 当前景区数据（spots/route/center/span） |
| `realMap` | Leaflet 地图实例（null=未初始化） |
| `visitorMarker` / `myLocMarker` | 游客红点 / 我的位置蓝点 |

### 16.5 排查顺序（出问题时照这个来）

1. 电脑浏览器 Ctrl+F5 打开线上地址，F12 → Console 看红色报错（第一手信息）；
2. 确认线上版本是最新：`curl -s 线上地址/index.html | grep 关键函数名`（如 toggleDemo 有输出=新版）；
3. 功能问题按 16.1 定位到函数，读代码 → 改 → 语法检查 → 部署（见第十五节命令）；
4. 部署后 CDN 有缓存，curl 加 `-H "Cache-Control: no-cache"` 验证；手机端强刷/重装；
5. 修完必须实测（CDP 真实点击或真机），**不许凭猜交付**。
